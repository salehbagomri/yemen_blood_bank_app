# 📓 مذكّرة تنفيذ — المرحلة 9: تنظيف التوثيق (أرشفة + مصدر حيّ واحد)

> **موجَّهة إلى:** وكيل الكود (المنفِّذ).
> **المخطِّط:** Claude.
> **مرتبطة بـ:** [QUALITY_REFINEMENT_PLAN.md](./QUALITY_REFINEMENT_PLAN.md) المرحلة 9.

---

## 🎯 السياق

التوثيق متأخّر عن الواقع بمسافة كبيرة (تقارير ديسمبر 2025 تصف مشروعاً قبل التحويل الوطني ومراجعة الجودة). النهج المعتمد من صاحب المشروع: **أرشفة التقارير القديمة + مصدر حيّ واحد + قسم قيود معروفة صادق**.

**اكتشافات حرجة أثناء المسح:**
1. `FIREBASE_UPDATE_REQUIRED.md` و`PROJECT_STATUS.md` يحملان **بصمات keystore القديم المحروق** (`D6:CD:53...`) الذي أبطلناه في المرحلة 8. (ملاحظة: البصمات ليست سرّية — المشكلة صحّة بيانات لا أمان، لكنها مضلّلة.)
2. `PROJECT_STATUS.md` يذكر keystore باسم `mahrah-release-key.jks` — اسم ثالث مختلف. فوضى توثيقية مؤكَّدة.

---

## 🧩 الأنماط المحمية

- لا حذف لأي ملف — **نقل للأرشيف** فقط (نحفظ الأثر التاريخي).
- لا تعديل لقيود `PROJECT_LOG.md` القديمة (السجل تاريخي — قراره معتمد: يبقى كما حدث).
- المصدر الحيّ يشير لـ pubspec للإصدار (لا أرقام ثابتة).
- لا إيموجي في الكود (الإيموجي في ملفات md مسموح).

---

## 📋 المهام

### المهمة 9.1 — إنشاء مجلد الأرشيف ونقل التقارير القديمة

أنشئ `docs/archive/` وانقل إليه (نقل لا حذف):
- `RELEASE_PREPARATION_REPORT.md`
- `ERROR_HANDLING_IMPLEMENTATION_REPORT.md`
- `FIREBASE_CONFIGURATION_REPORT.md`
- `FIREBASE_UPDATE_REQUIRED.md`
- `APP_BUNDLE_ISSUE.md`

أضف في رأس **كل** ملف مؤرشف (السطر الأول):
```
> 🗄️ **ملف مؤرشف — تاريخي لا يُعتمد.** المرجع الحيّ لحالة المشروع هو docs/PROJECT_STATUS.md.
> قد يحتوي معلومات قديمة (بصمات keystore محروق، أرقام إصدار/تحذيرات قديمة، أسماء حزم سابقة).
```

> **نقل عبر git للحفاظ على التاريخ:**
> ```
> git mv docs/RELEASE_PREPARATION_REPORT.md docs/archive/
> ... (لكل ملف)
> ```

---

### المهمة 9.2 — نقل مذكّرات المراجعة لمجلد فرعي منظّم

أنشئ `docs/quality-refinement/` وانقل إليه مذكّرات المراجعة:
- `BRIEF_phase3_reorder.md`, `BRIEF_phase4_closure.md`, `BRIEF_phase4_rls_audit.md`,
  `BRIEF_phase5_async_gaps.md`, `BRIEF_phase6_analyzer_cleanup.md`,
  `BRIEF_phase7_tests.md`, `BRIEF_phase8_security.md`
- و`QUALITY_REFINEMENT_PLAN.md` (يبقى الفهرس الرئيسي — انقله أيضاً أو أبقِه في docs/ مع إشارة).

> هذه أرشيف مسار المراجعة (مرجعية، ليست تاريخية-خاطئة). تنظيمها يخفّف فوضى `docs/`.

---

### المهمة 9.3 — إعادة كتابة PROJECT_STATUS.md كمصدر حيّ واحد

**استبدل محتوى الملف بالكامل** بحالة دقيقة حالية. الهيكل المقترح:

```markdown
# 📊 حالة المشروع — بنك دم اليمن

> **المصدر الحيّ الوحيد لحالة المشروع.** يُحدَّث بعد كل تغيير جوهري.
> الإصدار: انظر pubspec.yaml (مصدر الحقيقة). آخر تحديث: 2026-06-03.

## الحالة الحالية
- التطبيق: قيد التطوير (غير منشور على Google Play بعد).
- جودة الكود: flutter analyze = 0 issues. flutter test = 198 اختبار، 0 فشل. تغطية ~76% (منطق النماذج/الـ utils).
- النطاق: وطني (22 محافظة، 161 مديرية).
- المنصات: Android, iOS.

## ما أُنجز في مراجعة الجودة (2026-06-03)
- تأمين بيانات التوقيع (keystore جديد v2، إبطال القديم).
- تدقيق RLS باختبار اختراق فعلي (طبقة الكتابة محكمة).
- توحيد مصدر الإصدار (pubspec + قراءة ديناميكية).
- reorderBanners ذرّية عبر RPC.
- معالجة 12 BuildContext async gap.
- تنظيف analyze إلى صفر تام.
- إصلاح الاختبارات المهجورة + توسيع التغطية (198 اختبار).
- حذف MainActivity الشبح من مسار حزمة قديم.

## القيود المعروفة (Known Limitations) — بصراحة
- مفتاح Supabase anon علني في الكود (مقبول بطبيعته — الحماية على RLS؛ يُنظّف خارج الكود قبل النشر — المرحلة 8.5 المؤجَّلة).
- Rate limiting ضد السحب الجماعي للقراءة: غير مفعّل بعد (موصى به عبر Cloudflare Worker عند النشر).
- حماية تعديل المتبرع قائمة على ملكية الصف (added_by) لا المحافظة (آمن؛ قد يكون مقيّداً وظيفياً — قرار مفتوح).
- التغطية ~76% للمنطق القابل للاختبار وحدوياً؛ شاشات UI والخدمات الشبكية غير مغطّاة (تحتاج integration tests).
- بناء AAB: واجه سابقاً مشكلة (انظر الأرشيف)؛ يُعاد التحقق منه في مرحلة النشر.

## بيانات التوقيع
- keystore النافذ: yemen-release-key-v2.jks (غير مرفوع). بصمته موثّقة في yemen_blood_bank_handoff.md.
- ⚠️ أي بصمة في ملفات الأرشيف قديمة (keystore محروق) — لا تُعتمد.

## معلومات التطبيق
| المعلومة | القيمة |
|---------|--------|
| Package | com.bagomri.yemenbloodbank |
| الإصدار | انظر pubspec.yaml |
| الهدف | Android 5.0+ (API 21) |
| اللغة | العربية (RTL) |

## المراجع الحيّة
- CLAUDE.md — تعليمات الوكلاء والأنماط المحمية
- PROJECT_LOG.md — السجل الزمني
- yemen_blood_bank_handoff.md — البنية وSchema وRLS
- docs/quality-refinement/QUALITY_REFINEMENT_PLAN.md — خطة المراجعة
```

> اضبط التفاصيل حسب الواقع الفعلي. لا تذكر بصمة keystore الجديدة هنا (تبقى في handoff فقط).

---

### المهمة 9.4 — وسم الأدلة الإجرائية المتبقية

في رأس `PUBLISHING_GUIDE.md` و`FIREBASE_SETUP_GUIDE.md` (تبقى حيّة كأدلة إجراء):
```
> ℹ️ دليل إجرائي. لأرقام الإصدار/البصمات الحالية انظر pubspec.yaml وyemen_blood_bank_handoff.md.
> بعض الأمثلة هنا قد تذكر قيماً قديمة كتوضيح فقط.
```

---

### المهمة 9.5 — تحديث yemen_blood_bank_handoff.md بقسم القيود المعروفة

أضف قسماً «القيود المعروفة» (نفس قائمة 9.3) إن لم يكن موجوداً، ليكون مرجعاً تقنياً دائماً.

---

## ✅ قائمة التحقق (Definition of Done)

- [ ] `docs/archive/` يحوي التقارير القديمة الخمسة، كلها موسومة.
- [ ] `docs/quality-refinement/` يحوي مذكّرات المراجعة.
- [ ] `PROJECT_STATUS.md` أُعيدت كتابته كمصدر حيّ دقيق + قيود معروفة.
- [ ] الأدلة الإجرائية موسومة.
- [ ] `handoff` فيه قسم قيود معروفة.
- [ ] لا بصمة keystore محروق في أي ملف حيّ (فقط في الأرشيف الموسوم).
- [ ] `flutter analyze` = 0 (لم نلمس كوداً، لكن تأكيد).
- [ ] قيد PROJECT_LOG جديد يوثّق التنظيف.

---

## 📝 قيد PROJECT_LOG.md المقترح

```
### YYYY-MM-DD — [docs] تنظيف التوثيق: أرشفة التقارير القديمة + مصدر حيّ واحد
- **الوصف:** نقل 5 تقارير قديمة متناقضة (ديسمبر 2025) إلى docs/archive/ بوسم
  «تاريخي لا يُعتمد»، وتنظيم مذكّرات مراجعة الجودة في docs/quality-refinement/.
  إعادة كتابة PROJECT_STATUS.md كمصدر حيّ واحد دقيق يعكس الواقع (analyze=0،
  198 اختبار، نطاق وطني) مع قسم «القيود المعروفة» صريح. وسم الأدلة الإجرائية.
  إضافة قسم القيود المعروفة في handoff. إزالة الاعتماد على بصمات keystore
  المحروق من الملفات الحيّة (تبقى في الأرشيف الموسوم فقط).
- **الملفات:** docs/archive/* (منقولة), docs/quality-refinement/* (منقولة),
  docs/PROJECT_STATUS.md (إعادة كتابة), docs/PUBLISHING_GUIDE.md,
  docs/FIREBASE_SETUP_GUIDE.md, yemen_blood_bank_handoff.md
- **السبب/الدافع:** التوثيق كان متأخّراً عن الواقع ومتناقضاً ومضلّلاً لأي وكيل قادم.
- **اختبار:** لا كود (توثيق). analyze = 0 للتأكيد.
- **Commit:** `<hash>`
```

> **صيغة commit:** `docs: archive stale reports and establish single live status source`

---

## ⚠️ تنبيهات

1. **نقل لا حذف** (`git mv`) — نحفظ الأثر التاريخي وتاريخ git.
2. **لا تعدّل قيود PROJECT_LOG القديمة** — السجل تاريخي بقرار معتمد.
3. **لا تذكر بصمة keystore الجديدة في PROJECT_STATUS** — مكانها handoff فقط (تقليل أماكن الحقيقة).
4. لو وجدت ملفات md أخرى في جذر المشروع (لا docs/) تذكر حالة قديمة (مثل about_screen_improvement_plan)، أبلغني قبل التصرّف.
